## Uploading image to PHP api

Sending base64 encoded image to a php api

### Get started
1. Run:
    ``` 
    $ docker-compose up -d apache
    ``` 

2. Get in:
    ```
    http://localhost:3001
    ```